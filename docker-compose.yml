services:
  app:
    build: .
    container_name: agentopolis-app
    restart: unless-stopped
    networks:
      - web
    volumes:
      - agentopolis-data:/data       # funnel counters survive `up --build`

volumes:
  agentopolis-data:

networks:
  web:
    external: true
    name: web
