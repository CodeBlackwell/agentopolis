[project]
name = "botapest"
version = "0.1.0"
description = "The Grand Botapest Hotel — Habbo-style visualization of Claude Code agents"
requires-python = ">=3.11"
dependencies = ["fastapi>=0.110", "uvicorn>=0.29"]
